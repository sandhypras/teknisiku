name: teknisiku
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  cupertino_icons: ^1.0.8
  firebase_core: ^4.3.0
  firebase_messaging: ^16.1.0
  flutter:
    sdk: flutter
  flutter_dotenv: ^5.2.1
  flutter_local_notifications: ^19.5.0
  geocoding: ^4.0.0
  geolocator: ^14.0.3
  http: ^1.6.0
  image_picker: ^1.2.0
  supabase_flutter: ^2.9.0
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.3

flutter_launcher_icons:
  android: true
  ios: false
  image_path: "assets/logos/logoku.png"
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/logos/logoku.png"
  min_sdk_android: 21

flutter:
  uses-material-design: true

  assets:
    - .env
    - .env.example
    - assets/images/
    - assets/icons/
    - assets/logos/
    - assets/illustrations/
    - assets/placeholders/
